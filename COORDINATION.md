# Session-Koordination (parallele Claude-Code-Sessions)

Diese Datei koordiniert **mehrere gleichzeitig laufende Claude-Code-Sessions** im selben Repo.
Sie ist kein Ersatz für Git, sondern verhindert, dass zwei Sessions dieselben Dateien
gegenseitig überschreiben oder dasselbe Datenmodell in unterschiedliche Richtungen ändern.

> Es gibt **keine** Live-Verbindung zwischen den Sessions. Synchronisation passiert nur über
> (a) diese Datei, (b) häufige kleine Git-Commits. Jede Session liest diese Datei zu Beginn
> jeder Aufgabe und trägt ihren Anspruch ein, **bevor** sie schreibt.

## Protokoll (jede Session hält sich daran)

1. **Vor dem Editieren**: diese Datei lesen. Prüfen, ob eine andere Session die Datei/den
   Bereich beansprucht (Tabelle unten). Falls ja: nicht anfassen, sondern melden.
2. **Claim eintragen**: In der Tabelle „Aktive Ansprüche" eine Zeile mit Session-Kennung,
   Bereich, konkreten Dateien und Kurzstatus setzen, dann committen.
3. **Gemeinsame Dateien** (Liste unten) sind Hotspots: **immer** direkt vor dem Bearbeiten neu
   einlesen (`git pull --rebase` bzw. Datei frisch lesen). Nur additiv ändern wenn möglich,
   keine fremden Abschnitte umschreiben.
4. **Kleine, häufige Commits**. Nur die eigenen Dateien stagen (`git add <datei>`), **nie**
   `git add .` / `git add -A` (zieht fremde WIP-Änderungen mit). Commit-Message ohne
   Co-Authored-By-Zeile.
5. **Nach Fertigstellung**: Anspruch aus „Aktive Ansprüche" entfernen (oder auf „erledigt"
   setzen) und in „Änderungs-Log" eine Zeile ergänzen.
6. **Nie committen/pushen ohne ausdrückliche Aufforderung des Nutzers.**

## Aktive Ansprüche

| Session | Bereich | Dateien (Eigentum) | Status |
|---|---|---|---|
| S1 | Admin: Anlässe- + Mitgliederkarten-Tabellen (Backoffice-Blazor) | `Features/Ticketing/Admin/AdminEventsComponent.razor`, `AdminMemberCardsComponent.razor`, `MemberCardAdminReport.cs`, `EventAdmissionReport.cs`, `EventStatusEditor.cs`, `EventAdmissionQuota.cs`, `EventLinks.cs`, `Infrastructure/Ticketing/Admin/MemberCardAdminReportReader.cs`, `EventAdmissionReportReader.cs`, `UmbracoEventStatusEditor.cs`, `EventAdmissionQuotaEditor.cs`, `EventLinkReader.cs` | erledigt/kompiliert. Anlässe-Tabelle vollständig: Status + Einlasskontingent bearbeitbar (Overlay), Link-Kopierbuttons, alle Zähler inkl. Berechtigte gefüllt; Namensvereinheitlichung + Saison-Merk-Muster. Keine offenen Platzhalter mehr. |
| S1 | **Vereinheitlichung aller 5 Ticketing-Admin-Tabs (vom Nutzer ausdrücklich an S1: „S1 macht alle 5")** | NEU geteiltes CSS `wwwroot/css/ticketing-admin.css` + `<link>` in `Features/Ticketing/Admin/Views/Admin.cshtml`. Editiert ALLE fünf: `AdminEventsComponent`, `AdminMemberCardsComponent`, `AdminTicketsComponent`, `AdminSeasonCardsComponent`, `AdminFlexTicketsComponent`. NEU: `MemberCardEditor.cs` + `Infrastructure/.../MemberCardEditorAdapter.cs`, `FlexBundleEditor.cs` + `Infrastructure/.../FlexBundleEditorAdapter.cs`. | **erledigt/kompiliert** (Build 0 Fehler; `.razor` werden beim Build geprüft). Gemeinsame CSS-Quelle: gleiche Zeilen-/Headerhöhen, gleiche Dropdown-Breite, Toolbar (Aktion rechts der Filter), sortierbare Header, Edit-Icon-Spalte, Summary UNTER der Tabelle, einheitliches Overlay. Spieltickets-Saison jetzt via `TicketingAdminState` synchronisiert. Lokale `<style>`-Blöcke aus allen fünf entfernt. |
| S3 | Azure-Deploy + Umbraco-Media auf Azure Blob Storage | `Program.cs` (nur additive Zeile: Blob-Media-Provider), `Directory.Packages.props`, `RedAnts.csproj`, `deploy/azure-setup.sh`, `deploy/README.md`, `.github/workflows/deploy.yml` | erledigt (live, OIDC-Deploy grün, Blob-Media aktiv) |
| S3 | QR-Ticket-**Generierung** (vom Nutzer aus S5s Anspruch zugewiesen): signierter Token, QR-Bild, Web-Ticket-Seite. Vorerst ohne Wallet. | NEU, eigener Slice ohne fremde Dateien: `Features/Ticketing/Tickets/ITicketTokens.cs`, `Features/Ticketing/Tickets/WebTicketController.cs`, `Infrastructure/Ticketing/Tickets/TicketTokenService.cs`, `Infrastructure/Ticketing/Tickets/QrCodeRenderer.cs`, `Infrastructure/Ticketing/Tickets/TicketTokensComposer.cs` (eigener IComposer, NICHT `TicketingComposer.cs`), `Views/WebTicket.cshtml`; Paket QRCoder in `Directory.Packages.props`/`RedAnts.csproj` (S3-eigen). | erledigt/kompiliert (CoreCompile grün; Commit `a688bde`). Token-Contract für S5 im Änderungs-Log. Wallet noch offen. |
| S3 | Brevo-Mailversand (vom Nutzer aus S5s „Mailversand" zugewiesen): Sender-Service + Dev-Testendpunkt. Kein Auto-Versand. | NEU, eigener `Email/`-Slice, keine fremde Datei editiert: `Features/Ticketing/Email/IEmailSender.cs`, `Infrastructure/Ticketing/Email/BrevoEmailSender.cs`, `Infrastructure/Ticketing/Email/EmailComposer.cs`, `Features/Ticketing/Email/EmailTestController.cs` (nur Development). Liest `Infrastructure/Shared/EmailLayout.cs` read-only (S5). Key als User-Secret + Azure-App-Setting `Brevo:ApiKey`. | erledigt/kompiliert (CoreCompile grün; Commit `474bf5b`). Absender `Brevo:SenderEmail` muss noch auf eine in Brevo verifizierte Adresse gesetzt werden. |
| S3 | Öffentliche Saisonseite an Website-Startseite angleichen (vom Nutzer) | `Views/TicketSeason.cshtml` (nur diese Datei). Wechselt Layout `_TicketsLayout` → `_SiteLayout` und nutzt Website-Bausteine (Hero + Event-Liste). Referenziert `Views/Shared/_SiteLayout.cshtml` (S5) und `wwwroot/css/site.css`/`Views/Partials/Blocks/*` NUR read-only, editiert sie nicht. | erledigt (Commit `6ae6a1a`). Nutzt `_SiteLayout` + `.hero`/`.event-list`-Styles; `.cshtml` wird zur Laufzeit kompiliert (Seite neu laden genügt, kein Rebuild). |
| S3 | Anlass-Detailseite: Hero-Header (Bild + Titel) analog Startseite (vom Nutzer). S4s `TicketEvent.cshtml` ist „erledigt"/committet, Datei sauber; ich ändere nur den Kopfbereich additiv, S4s Warenkorb-Form + JS bleiben unangetastet. | `Views/TicketEvent.cshtml` (Kopf: `.hero` mit `eventImage` + `Model.Name` + Datum/Ort, `ViewBag.TransparentNav`; Body ohne das kontingentierte `.evt-image`/`.evt-title`). Nutzt `site.css` `.hero*` read-only. | erledigt (Commit `0e9202e`). Hero ~62vh; S4s Warenkorb-Form/JS unverändert; `.cshtml` runtime-kompiliert (Seite neu laden). |
| S3 | Anlässe: separater Dialog „Preise & Kontingente pro Kategorie" (vom Nutzer **ausdrücklich aus S1s `AdminEventsComponent.razor` an S3 reassigned**). | `Features/Ticketing/Admin/AdminEventsComponent.razor` (additiv: zweiter Overlay + Button, editiert Kategorie-Preise/Quota). Nutzt **S4s bestehenden Port `IEventPrices.GetByEventAsync`/`SaveAsync`** (Kategorie-Zeilen `SalePrice`/`Quota`) via DI — kein Schema/Port/Backend-Neubau. **S1: bitte `AdminEventsComponent.razor` bis zu meinem Abschluss NICHT parallel editieren** (Reassign durch Nutzer); ich lese frisch vor jedem Schreiben, ändere nur additiv, S1s Status-/Einlass-Overlay bleibt unangetastet. | erledigt (Commit `b56a73e`). Zweiter Overlay (💰-Button): Preis + Kontingent je `TicketCategory`, „Anbieten"-Checkbox; speichert via `IEventPrices.SaveAsync` und erhält Id + `TotalSalesQuota`/`AdmissionQuota` (S1s Einlass-Overlay unberührt). S1 kann `AdminEventsComponent.razor` wieder frei bearbeiten. |
| S2 | Admin-Ticketverwaltung + Namensvereinheitlichung Tickettypen (Backoffice-Blazor) | `Features/Ticketing/Admin/AdminTicketsComponent.razor`, `Features/Ticketing/Admin/TicketTypeDisplay.cs` (NEU), `Features/Ticketing/Admin/TicketingAdminComponent.razor` (Tab-Labels) | erledigt/kompiliert |
| S5 | QR-Ticketzustellung (signierter Token, Scan-Endpoint, Web-Ticket mit Live-QR, Mail-QR, Google Wallet) | `Features/Ticketing/Scanning/*`, `Features/Ticketing/Public/*` (Web-Ticket), `Infrastructure/Shared/EmailLayout.cs` + Mailversand, neue Token-/QR-Helfer; ausserdem `Views/Shared/_SiteLayout.cshtml` (Warenkorb-Razor-Fix, uncommittet) | in Arbeit |
| S1 | **Scanner-Einlasslogik (vom Nutzer an S1 reassigned aus S5s Scanner: „S1 übernimmt")** | `Features/Ticketing/Scanning/ScanView.razor` (In/Out-Toggle, Freieinlass-Buttons, Restkapazität-Banner), NEU `Features/Ticketing/Scanning/IAdmissionService.cs` (Port) + `Infrastructure/Ticketing/Scanning/AdmissionService.cs` (Adapter, schreibt `TicketEventVisits`/`TicketEventVisitsLogs`, liest `EventPrices.AdmissionQuota`, nutzt S3s `IIssuedTicketReader`). **S5: bitte `ScanView.razor`/`Scanning/*` bis auf Weiteres NICHT parallel editieren** (Reassign durch Nutzer). Nutzt S4-Tabellen nur über eigene SQL-Adapter, keine fremde Datei editiert. | in Arbeit |
| S5 | Beispieldaten Saison-/Mitgliederkarten (idempotenter Boot-Seeder) | `Infrastructure/Ticketing/Seeding/SampleCardsSeeder.cs` (NEU, eigener Ordner ausserhalb S4-`Sales/*`); referenziert nur lesend S4-Typen + Enums, editiert keine fremde Datei | in Arbeit |
| S4 | Ticketing Sales/Pricing-Datenmodell + Doku | `Domain/Ticketing/Sales/*`, `Infrastructure/Ticketing/Sales/*`, `Infrastructure/Ticketing/TicketingMigration.cs`, `Features/Ticketing/Ports/SalesCatalogPorts.cs`, `Features/Ticketing/Cart/*`, `Infrastructure/Ticketing/SessionCartService.cs`, `Views/TicketEvent.cshtml`, `README.md`, `CLAUDE.md`, `ARCHITECTURE.md` | erledigt (Single-Step-Schema, Enums als int, Doku aktuell) |
| S4 | Admin: gemerkte Saison über Tabs + Reload beim Tab-Wechsel (Foundation) | `Features/Ticketing/Admin/TicketingAdminState.cs` (NEU), `Features/Ticketing/Admin/TicketingAdminManifest.cs` (nur additive DI-Zeile), `Features/Ticketing/Admin/AdminSeasonCardsComponent.razor` (Saisonkarten-Tab verdrahtet) | Foundation erledigt; S1/S2-Tabs ziehen nach (siehe Änderungs-Log) |
| S4 | Saisonkarten bearbeitbar (Edit-Button + Overlay: Kategorie/Preis/Status) | `Features/Ticketing/Admin/AdminSeasonCardsComponent.razor` (Overlay), `Features/Ticketing/Admin/SeasonPassAdminReport.cs` (Read-Modell), `Infrastructure/Ticketing/Admin/SeasonPassAdminReportReader.cs` (Projektion), `Domain/Ticketing/Sales/SeasonPass.cs` (Edit-Mutator), `Features/Ticketing/Ports/SalesCatalogPorts.cs` (`ISeasonPasses`), `Infrastructure/Ticketing/Sales/SeasonPassRepository.cs` (NEU), `Infrastructure/Ticketing/TicketingComposer.cs` (DI-Zeile) | erledigt/kompiliert (razor 0 Fehler) |
| S4 | "In den Warenkorb" ohne Reload: Inline-Feedback + kein Scrollsprung (AJAX) | `Features/Ticketing/Cart/CartController.cs` (Add gibt bei fetch JSON zurück), `Views/TicketEvent.cshtml` (fetch-Handler + Feedback). Liest zur Laufzeit S5s Nav-Badge per CSS-Klasse `.nav-cart`/`.nav-cart-badge` (editiert `_SiteLayout.cshtml` NICHT) | erledigt/kompiliert (razor 0 Fehler) |
| S5 | Spieltickets: Ticket-Erstellung im Reiter (Button + Overlay: Kategorie/Abgabepreis) | `Features/Ticketing/Admin/AdminTicketsComponent.razor` (von S2 ausdrücklich zugewiesen); nutzt bestehende `IEventTickets.SaveAsync` + `EventTicket.Create` via DI, kein Schema/Port/neue Datei | in Arbeit |
| S4 | Spieltickets: Link „Online-Ticket" pro Zeile (vom Nutzer aus S5s `AdminTicketsComponent.razor` zugewiesen) | `Features/Ticketing/Admin/AdminTicketsComponent.razor` (nur additiv: ein `<a href="/ticket/for/{uuid}">` in der Aktionen-Spalte + `.ta-btn-link`-CSS). Nutzt S3s bestehende Route `/ticket/for/{uuid}`, keine Backend-Änderung | erledigt/kompiliert (razor 0 Fehler) |
| S2 | Flextickets-Slice: Menüpunkt + Bundle-Entität + Overlay (vom Nutzer freigegeben „S2 baut alles") | NEU: `Domain/Ticketing/Sales/FlexTicketBundle.cs`, `Infrastructure/Ticketing/Sales/FlexTicketRecords.cs`, `Infrastructure/Ticketing/Sales/FlexTicketBundleRepository.cs`, `Features/Ticketing/Ports/FlexTicketPorts.cs`, `Features/Ticketing/Admin/AdminFlexTicketsComponent.razor`. EDIT (additiv, mit S4 abgestimmt via Log): `Infrastructure/Ticketing/Sales/SalesRecords.cs` (+Spalte `SeasonSingleTickets.BundleId`), `Domain/Ticketing/Sales/SeasonSingleTicket.cs` (+`BundleId`/`CreateForBundle`), `Infrastructure/Ticketing/TicketingMigration.cs` (+Tabelle `FlexTicketBundles`), `Infrastructure/Ticketing/TicketingComposer.cs` (+DI-Zeile), `Features/Ticketing/Admin/TicketingAdminComponent.razor` (+Tab „Flextickets"); Migration jetzt mit inkrementellem Schritt `AddFlexTicketBundles` | erledigt/kompiliert (keine CS/RZ-Fehler). **DB muss NICHT mehr gelöscht werden** — der neue Migrationsschritt legt Tabelle/Spalte in bestehenden DBs idempotent nach. |
| S5 | Idempotenter Schema-Bau (vom Nutzer aus S4 zugewiesen) | `Infrastructure/Ticketing/TicketingMigration.cs` (jede Tabelle existenz-geschützt; Plan läuft bei jedem Boot neu, Migrationsstand wird zurückgesetzt) | in Arbeit |
| S2 | Mitglieder-CSV-Import + Preis-Entfernung (vom Nutzer freigegeben „S2 baut alles") | NEU: `Features/Ticketing/Ports/MemberCardPorts.cs`, `Infrastructure/Ticketing/Sales/MemberCardRepository.cs`, `Features/Ticketing/Admin/MemberCsv.cs`, `Features/Ticketing/Admin/MemberImportController.cs`. EDIT (übernommen, via Log abgestimmt): `Domain/Ticketing/Sales/MemberCard.cs` (Price raus, `Reference` rein, `Create`/`FromPersistence` neu), `Infrastructure/Ticketing/Sales/SalesRecords.cs` (`MemberCardRecord`: Price raus, `Reference` rein), `Features/Ticketing/Admin/MemberCardAdminReport.cs` (`MemberCardListItem`: Price raus, `Reference` rein), `Infrastructure/Ticketing/Admin/MemberCardAdminReportReader.cs` (Projektion), `Infrastructure/Ticketing/Seeding/SampleCardsSeeder.cs` (S5-Seeder ohne Price), `Features/Ticketing/Admin/AdminMemberCardsComponent.razor` (Import-Overlay + Referenz-Spalte, Preis-Spalte raus), `Infrastructure/Ticketing/TicketingMigration.cs` (+Schritt `AdjustMemberCardColumns`), `Infrastructure/Ticketing/TicketingComposer.cs` (+DI) | erledigt/kompiliert (keine CS/RZ-Fehler) |
| S5 | Live-Test-Schutz: Basic-Auth-Gate („alles dicht") + Host-Routing für `tickets.redants.ch`/`scan.redants.ch` (vom Nutzer aus S3s `Program.cs` zugewiesen) | `Program.cs` (nur additiv: zwei Middlewares vor `UseStaticFiles`; Basic-Gate nur aktiv wenn `BasicAuth:Username`/`:Password` gesetzt, lokal also aus) | in Arbeit |
| S5 | Mitglieder-Export: Karten je Referenz als CSV (Import-Spalten + QR-URL-Spalte für Serienbrief; vom Nutzer zugewiesen, Variante B) | NEU: `Features/Ticketing/Admin/MemberExportController.cs` (Endpoints `/admin/mitglieder/referenzen` + `/admin/mitglieder/export.csv`); liest `MembershipCards` read-only + `ITicketTokens` (S3). Button/Dialog im Mitglieder-Tab **bewusst weggelassen**, solange S1 die 5 Admin-Komponenten sperrt | in Arbeit |
| S4 | Guest-Checkout (Pseudo): Adresse → Zahlungsart → Bestätigung, Verkauf immer erfolgreich | NEU: `Features/Ticketing/Cart/CheckoutController.cs`, `Features/Ticketing/Cart/CheckoutModels.cs`, `Views/Checkout/*` (`_Summary`/`Address`/`Payment`/`Confirmation`), `Infrastructure/Ticketing/Sales/OrderRepository.cs`. EDIT (eigene/additive): `Features/Ticketing/Ports/SalesCatalogPorts.cs` (+`IOrders`), `Infrastructure/Ticketing/TicketingComposer.cs` (+DI-Zeile), `Views/Cart/Index.cshtml` (Button „Zur Kasse") | erledigt/kompiliert (0 CS/RZ-Fehler) |
| S2 | BUGFIX „Anlass bearbeiten hängt beim Speichern" (vom Nutzer ausdrücklich S2 zugewiesen, aus S1s Datei) | `Infrastructure/Ticketing/Admin/UmbracoEventStatusEditor.cs` (nur diese Methode: `SetStatusAsync` in `Task.Run` gewickelt) | erledigt/kompiliert; Rebuild+Neustart nötig, dann testen |
| S4 | BUGFIX „/umbraco-Login-Schleife hinter Basic-Gate" (vom Nutzer ausdrücklich S4 zugewiesen, aus S5s `Program.cs`) | `Program.cs` (nur additiv im Basic-Gate: `/umbraco` vor dem Basic-Check durchlassen) | erledigt, deployed (Lauf `28686922373` grün) |
| S4 | BUGFIX „Deploy rot am Warm-up-Step" (vom Nutzer zugewiesen, aus S3s `deploy.yml`) | `.github/workflows/deploy.yml` (nur der „Warm up the site"-Step: `200`/`401` gelten als oben, curls nicht-fatal via `\|\| echo 000`) | erledigt |
| S2 | Mitgliederkategorie (Red Ants / Block 4) statt Ticketkategorie + Online-Logo (vom Nutzer „Mach alles") | NEU: `Domain/Ticketing/Sales/MemberCategory.cs`, `wwwroot/img/members/red-ants.webp` + `block4.webp`. EDIT (übernommen, via Log abgestimmt): `Domain/Ticketing/Sales/MemberCard.cs`, `Infrastructure/Ticketing/Sales/MemberCardRepository.cs`, `Features/Ticketing/Ports/MemberCardPorts.cs`, `Features/Ticketing/Admin/MemberCardAdminReport.cs` + `MemberCardEditor.cs` + `AdminMemberCardsComponent.razor`, `Infrastructure/Ticketing/Admin/MemberCardAdminReportReader.cs` + `MemberCardEditorAdapter.cs`, `Infrastructure/Ticketing/Seeding/SampleCardsSeeder.cs`, `Features/Ticketing/Tickets/ITicketTokens.cs` + `WebTicketController.cs`, `Infrastructure/Ticketing/Tickets/IssuedTicketReader.cs`, `Views/WebTicket.cshtml` | erledigt/kompiliert (keine CS/RZ-Fehler) |
| S2 | Cookie-Gate statt Basic-Auth (Live-Test-Schutz, vom Nutzer S2 zugewiesen, aus S5s Gate) | `Program.cs` (Gate-Block ersetzt: HTML-Login `/__gate` + signiertes DataProtection-Cookie statt `WWW-Authenticate`) | erledigt/kompiliert |
| S2 | Neuer Admin-Tab „Saisons" (vor Anlässe): Status/Kontingent/Preise + Links (vom Nutzer „S2 baut alles") | NEU: `Features/Ticketing/Admin/AdminSeasonsComponent.razor`, `Features/Ticketing/Admin/SeasonStatusEditor.cs`, `Features/Ticketing/Admin/SeasonLinks.cs`, `Infrastructure/Ticketing/Admin/UmbracoSeasonStatusEditor.cs`, `Infrastructure/Ticketing/Admin/SeasonLinkReader.cs`. EDIT (S4-Pricing übernommen): `Domain/Ticketing/Sales/Pricing.cs` (`SeasonPrice`+`TotalSalesQuota`), `Infrastructure/Ticketing/Sales/SalesCatalogRecords.cs` (`SeasonPriceRecord`+Spalte), `SalesCatalogRepositories.cs` (Map/Save), `PricingCatalogSeeder.cs` (Aufruf), `TicketingMigration.cs` (+Schritt `AddSeasonPriceTotalQuota`), `TicketingAdminComponent.razor` (Tab+Default) | erledigt/kompiliert (keine CS/RZ-Fehler) |

## Gemeinsame Dateien (Hotspots — vor dem Editieren immer neu einlesen)

Diese Dateien werden von mehreren Bereichen berührt. Änderungen hier bitte ankündigen und
möglichst additiv halten.

- `Domain/Ticketing/Sales/SalesEnums.cs` — Enums (TicketType, TicketCategory, TicketStatus, …)
- `Infrastructure/Ticketing/Sales/SalesRecords.cs` — NPoco-Records (Tabellen-Schema)
- `Infrastructure/Ticketing/TicketingMigration.cs` — Schema-Erstellung (Single-Step)
- `Features/Ticketing/Ports/CatalogPorts.cs`, `SalesCatalogPorts.cs` — Ports
- `Infrastructure/Ticketing/TicketingAliases.cs` — Content-Type-/Property-Aliases
- `Infrastructure/Ticketing/Content/TicketingContentTypeSeeder.cs` — Content-Type-Seeding
- `Program.cs`, `appsettings.json` — App-Bootstrap/Konfiguration
- `README.md`, `CLAUDE.md` — Doku/Projektregeln

**Vereinbarung zum Datenmodell**: Enums werden als **Integer** persistiert; Records verwenden
`int`-Spalten (`Category`, `Status`, `TicketType`, …), Labels über `DisplayName()`-Extensions.
Wer das Schema ändert, trägt es sofort ins „Änderungs-Log" ein, damit die anderen Sessions ihre
Reader/Records anpassen.

## Änderungs-Log (Schema/Contracts, die andere betreffen)

Nur Änderungen eintragen, die **andere Sessions** betreffen (Schema, Enums, Port-Signaturen,
gemeinsame DI-Registrierungen). Neueste zuerst.

| Datum | Session | Was geändert wurde | Auswirkung auf andere |
|---|---|---|---|
| _(TT.MM.JJJJ)_ | _S?_ | _z. B. „MembershipCards.Status von string auf int"_ | _z. B. „Reader müssen (TicketStatus)-Cast nutzen"_ |
| 05.07.2026 | S4 | **Käufer (Privatperson/Firma) + „Erstellt von" auf dem Verkaufsmodell** (großes Feature vom Nutzer S4). NEU: `Domain/Ticketing/Sales/Buyer.cs` (Value-Object) + `BuyerType`-Enum in `SalesEnums.cs` (additiv). `EventTicket`/`SeasonPass` haben jetzt `Buyer? Buyer` + `string? CreatedByName` (Create/FromPersistence um **optionale** Trailing-Params erweitert, alte Aufrufe kompilieren weiter). `FlexTicketBundle`/`MemberCard` haben `CreatedByName` (optional). `BillingAddress.Create`/`FromPersistence` **Signatur geändert** (führend `BuyerType type` + `string? company`) — Aufrufer angepasst (`CheckoutController`, `OrderRepository`, `TicketingMappers`). Schema: neue **nullable** Spalten via Migrationsschritt `AddBuyerAndCreatorColumns` (ColumnExists-Muster) auf `EventTickets`/`SeasonPasses` (BuyerType,BuyerFirstName,BuyerLastName,BuyerCompany,CreatedByName), `Orders` (BillingType,BillingCompany), `MembershipCards`/`FlexTicketBundles` (CreatedByName). Ports: `IMemberCards.ImportAsync` und `IFlexTicketBundles.CreateAsync` haben je einen **optionalen** `createdByName`-Param; `FlexTicketBundleView` + `SeasonPassListItem` + `MemberCardListItem` um optionale Felder erweitert. Checkout schreibt den Käufer auf jedes EventTicket + `CreatedByName="Online-Kauf"`; Adressseite hat Privat/Firma-Umschalter. **Admin-UI (Spalten + Create-Dialoge) folgt in einem zweiten Commit.** | **ALLE Verkaufs-/Admin-Sessions (S1/S2/S5)**: Rein additiv bei Enums/Records/Ports (alte Aufrufe kompilieren). Nur `BillingAddress.Create/FromPersistence` ist eine echte Signaturänderung (alle Aufrufer von mir angepasst). Wer neue Records konstruiert: `FlexTicketBundleView`/`SeasonPassListItem`/`MemberCardListItem` haben optionale Zusatzfelder am Ende. DB: nur additive nullable Spalten, kein Drop, kein Enum-Wert entfernt. Bestehende `EventTicket.Create(...)`/`SeasonPass.Create(...)`-Aufrufe unverändert gültig. |
| 05.07.2026 | S3 | **Aktions-Buttons in den Admin-Tabellen mit Abstand** (vom Nutzer, Commit `375cba4`): additive Regel in `wwwroot/css/ticketing-admin.css` — `.ta-col-actions > * + *, .ta-links > * + * { margin-left: 1rem; }` (≥ halbe 30px-Icon-Buttonbreite gegen Verklicken). Zentral im geteilten CSS (nicht komponentenlokal, wie von S1 gewünscht). | **S1** (CSS-Eigner): rein additive Zeile am Ende von `ticketing-admin.css`, bitte behalten; betrifft alle 5 Tabs (Events 💰+✎, andere QR+✎). Kein Schema/Port/DB. |
| 05.07.2026 | S5 | **Saisonkarte im Admin erstellen** + **Block-4-Karten optisch abgehoben** (vom Nutzer S5). (1) `AdminSeasonCardsComponent.razor`: „Saisonkarte erstellen"-Button + Overlay (Kategorie + Preis), speichert via bestehendem `ISeasonPasses.SaveAsync(SeasonPass.Create(...))`, analog zum Spieltickets-Overlay, rein additiv. (2) Block 4 vs. Red Ants sichtbar: neue Badge-Klassen `.ta-badge-block4` (schwarz/gold) + `.ta-badge-redants` in `wwwroot/css/ticketing-admin.css`; `AdminMemberCardsComponent.razor` zeigt die Kategorie als farbiges Badge; `Views/WebTicket.cshtml` gibt Block-4-Karten einen schwarz/goldenen Kopf (`wt-head--block4`), Red Ants bleibt grün. | **S1** (Admin-Tab-/CSS-Eigner): additive Badge-Klassen + Kategorie-Badge in Mitglieder-/Saisonkarten-Tab, bitte frisch einlesen. **S3** (Ticket-View): `WebTicket.cshtml` nur um Block-4-Kopf ergänzt. Kein Schema/Enum/Port. |
| 05.07.2026 | S4 | **Flextickets: Word-Serienbrief (RTF) entfernt, Referenz in den CSV/Excel-Export aufgenommen** (vom Nutzer S4 zugewiesen, aus S2s Flextickets-Slice). `FlexBundleExportController.cs`: `Serienbrief`-Action + `QrPicture`-Helfer entfernt, `IQrCodeRenderer`-Ctor-Dependency raus; CSV-Header jetzt `Referenz;Kurzkennung;Link` (mit CSV-Quoting). `AdminFlexTicketsComponent.razor`: 📄-Serienbrief-Button entfernt (nur der `<a>`; Rest unangetastet). `FlexBundleExport.cs`: Record `FlexBundleTicket` um `string Reference` erweitert. `FlexBundleTicketsAdapter.cs`: SQL joint jetzt `FlexTicketBundles` und liefert `Reference`. | **S2** (Flextickets-Eigner): `FlexBundleTicket` ist jetzt `(Guid Uuid, int SeasonId, string Reference)` — falls ihr das Record woanders erzeugt, dritten Wert ergänzen (aktuell nur Controller+Adapter, beide von mir angepasst). Route `/serienbrief.rtf` existiert nicht mehr. Kein Schema/Enum/DB/Port geändert. |
| 05.07.2026 | S5 | **Automatischer Ticket-Mailversand beim Kauf** (vom Nutzer S5). Brevo produktiv eingerichtet: Absender `tickets@redants.ch`, Domain `redants.ch` in Azure DNS via DKIM authentifiziert (bestehende O365/Fairgate-SPF unangetastet), `Brevo:ApiKey` als user-secret + App-Setting auf `app-redants-dev`/`-prod`, `Brevo:SenderEmail` in `appsettings.json`. Neuer eigener Slice: `Features/Ticketing/Email/IOrderMailer.cs` (+ `OrderMailModel`/`OrderMailTicket`), `Infrastructure/Ticketing/Email/OrderMailer.cs` (baut QR-PNG via `IQrCodeRenderer` + Token via `ITicketTokens`, HTML via `EmailLayout`, sendet via `IEmailSender`), `Infrastructure/Ticketing/Email/OrderMailerComposer.cs` (eigener IComposer, scoped). `EmailLayout.cs` optisch an die Website angeglichen (Logo-Header + Oswald, Signatur unverändert). `Views/WebTicket.cshtml` um `@@media print` ergänzt (druckfreundlich, kein PDF). **S4s `CheckoutController.Pay` angefasst** (additiv): ein Konstruktor-Param `IOrderMailer` + Sende-Aufruf nach der Ticket-Erzeugung. | **S4** (Checkout-Eigner): `CheckoutController` hat neu `IOrderMailer` im Primary Constructor und sendet nach dem Kauf; bitte vor Weiterarbeit frisch einlesen, sonst additiv. **S3** (Email/Ticket-Slice): `EmailLayout.Render`-Signatur unverändert (Testcontroller läuft weiter); `WebTicket.cshtml` nur um Print-CSS ergänzt. **Hinweis**: Mail feuert mangels echtem Payrexx-Webhook auch bei „Rechnung"/„Bar". Google Wallet noch offen (braucht Issuer-Konto). Kein Schema/Enum/Port. |
| 05.07.2026 | S4 | **BUGFIX „/umbraco-Backoffice fragt endlos nach Basic-Auth"** (vom Nutzer S4 zugewiesen, aus S5s Basic-Gate in `Program.cs`). Ursache: der Blazor-Server-Admin (`/admin/ticketing`-iframe, `TicketingAdminController` `[Authorize]` BackOffice) lädt Ressourcen **ausserhalb** `/umbraco` (`/_blazor`-SignalR-Hub, `/_framework/blazor.server.js`), die der Basic-Gate mit 401+`WWW-Authenticate` beantwortete → Basic-Dialog poppt im Backoffice immer wieder. Fix: Bypass im Gate additiv erweitert um `/admin/ticketing`, `/_blazor`, `/_framework` (neben dem bestehenden `/umbraco`). `/admin/mitglieder/*` bleibt bewusst gegated (S5s `MemberExportController` hat keine eigene Auth). Nur diese eine `if`-Bedingung geändert, sonst nichts. | **S5** (Gate-Eigner): Bypass-Liste bitte so lassen. **Hinweis**: `MemberExportController` (`/admin/mitglieder/referenzen`, `/export.csv`) trägt kein `[Authorize]` — aktuell schützt ihn nur der Basic-Gate. Für den Regelbetrieb ohne Basic-Gate dort `[Authorize(BackOffice)]` ergänzen, sonst sind Mitgliederdaten offen. Kein Schema/Enum/Port. |
| 04.07.2026 | S3 | **SQLite komplett entfernt + Dev/Prod auf Azure SQL** (vom Nutzer, neues 2-Stage-Setup). Paket `Umbraco.Cms.Persistence.Sqlite` aus `RedAnts.csproj`/`Directory.Packages.props` entfernt; SQLite-WAL-Bootstrap-Block in `Program.cs` gelöscht (nur dieser Block, S5s Basic-Auth/Host-Routing unangetastet). `appsettings.Development.json` nutzt jetzt Provider `Microsoft.Data.SqlClient` mit leerem DSN. Neue Azure-Infra (Switzerland North): App Services `app-redants-dev` (B1) / `app-redants-prod` (B3), 1 SQL-Server `sql-redants-ch` mit 2 DBs (`sqldb-redants-dev` Basic / `sqldb-redants-prod` S1), 2 Storage-Konten, 4 Custom-Domains, Basic-Auth-Gate auf beiden. Workflow neu: `feature/**`→Dev, `main`→Dev+Prod (GitHub Environments). | **ALLE**: Lokale Entwicklung läuft **nicht mehr auf SQLite**, sondern auf der **gemeinsamen Dev-Azure-SQL**. Nötig pro Dev-Maschine: `dotnet user-secrets set ConnectionStrings:umbracoDbDSN "<Dev-DSN>"` (+ Provider `Microsoft.Data.SqlClient`, Blob-Conn) und eine SQL-Firewall-Regel für die eigene IP. Den Dev-DSN aus den App-Settings von `app-redants-dev` ziehen. **Migrations-Eigner (S2/S4)**: Die `TicketingMigration`-`isSqlite`-Zweige laufen jetzt nie mehr (SqlClient) — der SqlClient-/Builder-Pfad muss auf echtem SQL Server funktionieren (bisher nur gegen SQLite getestet). Kein Schema/Enum/Port geändert. |
| 04.07.2026 | S3 → **Aufgabe/Bitte an S1** | **QR-Symbol-Spalte „Online-Ticket / Zugriffskarte" in den Admin-Ticketlisten** (vom Nutzer; S1 macht es, da S1 die 5 Tabs vereinheitlicht). In **Spieltickets** (`AdminTicketsComponent`, ersetzt S4s Text-Link „Online-Ticket" durch das QR-Icon), **Saisonkarten** (`AdminSeasonCardsComponent`) und **Mitglieder** (`AdminMemberCardsComponent`) je eine Spalte mit einem QR-Icon, das auf die externe Zugriffskarte verlinkt. **NICHT** bei Flextickets. Link-Ziel = meine bestehende Route `GET /ticket/for/{uuid}` (funktioniert für EventTicket/SeasonPass/MemberCard, leitet auf die signierte Web-Ticket-Seite `/ticket/{token}` weiter). Voraussetzung: die jeweilige Row muss die Ticket-`Guid Uuid` liefern (SeasonPassListItem hat `Uuid`; für Mitglieder ggf. `Uuid` ins Read-Modell aufnehmen). **Fertiger Snippet** (row-Variable `item` anpassen): `<a class="ta-qr-link" href="/ticket/for/@item.Uuid" target="_blank" title="Online-Ticket" aria-label="Online-Ticket"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><path d="M17 14h.01M20 14v3M14 17h.01M17 20h3M20 20h.01"/></svg></a>` + CSS `.ta-qr-link{color:var(--red,#C8102E);display:inline-flex}` (ins geteilte `ticketing-admin.css`). | **S1** (Tab-Owner): Umsetzung liegt bei euch (kein Code von mir an euren Dateien). Kein Schema/Enum/Port. Meine Route/`IIssuedTicketReader` sind fertig; falls `MemberCardListItem` keine `Uuid` hat, bitte ergänzen (S1-Read-Modell). |
| 04.07.2026 | S3 | **Header-Höhe Saison-/Anlass-Seite an Startseite angeglichen** (vom Nutzer, Commit `40e12c6`): Die Live-Startseite `/` nutzt als Kopf das `headerElement` → `.page-header { min-height: 300px }`, NICHT den 100svh-Hero. `TicketSeason.cshtml` + `TicketEvent.cshtml` liefen aber auf `.hero` mit `height: 52vh` (≈468px, höher als die Startseite). Beide jetzt auf identisches `.page-header`-Markup+CSS umgestellt (300px, transparente Nav darüber, gleiche Titelgrösse + roter Akzent, plus `.page-header-date`). Verifiziert via gerendertem HTML: alle drei Seiten `min-height: 300px`. | **An die Session hinter `59504f9`** („Align ticket page headers with the homepage hero"): Der Homepage-Kopf ist das `page-header`-Band, nicht der Hero — bitte den 52vh-`.hero` auf den Seiten NICHT wiederherstellen. Team-Logos in der Event-Liste (aus `59504f9`) unverändert gelassen. Kein Schema/Enum/Port. |
| 03.07.2026 | S3 | **Erledigt** (S2-Aufgabe, Commit `0ee260d`): Saisonseite (`Views/TicketSeason.cshtml`) vom Vollbild-Hero auf ein kompaktes Header-Bild-Band umgestellt (feste Höhe ~220–320px, Titel + Datum über dem Bild, Gradient-Scrim, deckende Nav, keine Scroll-Leiste). Event-Liste unverändert. | Keine (nur meine Datei, Razor runtime-kompiliert → Seite neu laden). Kein Schema/Enum/Port. Offen: Anlass-Detailseite (`TicketEvent.cshtml`) hat noch den ~62vh-Hero; auf Wunsch gleiche kompakte Band-Optik. |
| 03.07.2026 | S5 | **Basic-Auth-Gate + Host-Routing in `Program.cs`** (Live-Test-Schutz, vom Nutzer aus S3 zugewiesen). Zwei additive Middlewares vor `UseStaticFiles`: (1) HTTP-Basic-Gate über ALLE Requests, aktiv nur wenn `BasicAuth:Username` **und** `BasicAuth:Password` gesetzt sind (lokal aus, kein Dev-Impact); (2) Root-Redirect je Host: `scan.*`→`/scanntickets`, `tickets.*`→`/tickets`. Keine bestehende Zeile geändert. | **S3** (Deploy-Eigner): Für den Live-Test in Azure setzen: App-Settings `BasicAuth__Username`/`BasicAuth__Password`, und Custom Domains `tickets.redants.ch`/`scan.redants.ch` auf `app-redants` binden (+ Managed-Cert). Basic gilt dann für die ganze Site inkl. Backoffice, Ticket-Links und Webhooks (bewusst „alles dicht"). Kein Schema/Enum/Port. |
| 03.07.2026 | S5 | **Schema-Bau idempotent gemacht** (`TicketingMigration.cs`, vom Nutzer aus S4 zugewiesen). `CreateTicketingSchema` legt jede Tabelle nur noch bei Fehlen an (`TableExists`-Guard je Tabelle via `EnsureTable<Record>("Name")`); `TicketingMigrationComponent` setzt den Migrationsstand vor jedem Lauf zurück (`keyValueService.SetValue(upgrader.StateValueKey, "")`), sodass der komplett geschützte Plan bei **jedem** Boot neu läuft und fehlende Tabellen/Spalten idempotent nachzieht. `AddFlexTicketBundles` (S2) bleibt unverändert erhalten. | **Alle**: DB-Reset bei additiven Schemaänderungen ist nicht mehr nötig, eine neue Tabelle/Spalte erscheint beim nächsten Start automatisch. **S2/S4**: Das von S2 dokumentierte Muster „für Bestandsänderungen NICHT `CreateTicketingSchema` anfassen" ist damit hinfällig; neue Tabellen einfach in `CreateTicketingSchema` mit `EnsureTable<Record>("Name")` ergänzen. **Doku (S4)**: CLAUDE.md/README beschreiben noch „Single-Step, DB löschen" — bitte anpassen (gehört euren Doku-Dateien). Kein Enum/Port/Spaltenänderung. |
| 03.07.2026 | S3 | **Brevo-Mailversand übernommen (aus S5s „Mailversand", vom Nutzer zugewiesen).** Neuer `Email/`-Slice: Port `IEmailSender.SendAsync(to, name, subject, html)` (`Features/Ticketing/Email/`), `BrevoEmailSender` (Brevo REST, Key aus `Brevo:ApiKey`), eigener `EmailComposer` (registriert `AddHttpClient("brevo")` + `IEmailSender`), Dev-Endpoint `GET /dev/test-mail?to=&uuid=` (nur Development). `EmailLayout.Render` nur read-only genutzt. | **S5**: bitte KEINEN eigenen Mail-Client bauen — `IEmailSender` injizieren und `SendAsync(...)` mit `EmailLayout.Render(...)`-HTML aufrufen; für Ticket-Mails den QR via `IQrCodeRenderer.RenderPngDataUri(url)` einbetten (siehe `EmailTestController` als Vorlage). Kein Schema/Enum/bestehender Port geändert. `Brevo:ApiKey` liegt als User-Secret (dev) + App-Setting (prod); `Brevo:SenderEmail` muss auf eine verifizierte Brevo-Adresse gesetzt werden. |
| 03.07.2026 | S3 | **QR-Ticket-Generierung übernommen (aus S5s Anspruch, vom Nutzer zugewiesen).** Neuer eigener Slice, keine fremde Datei editiert: `ITicketTokens`/`IQrCodeRenderer`/`IIssuedTicketReader` (Ports), `TicketTokenService`, `QrCodeRenderer` (QRCoder 1.8.0), `IssuedTicketReader` (liest S4-Records read-only), `TicketTokensComposer` (eigener IComposer), `WebTicketController` (`/ticket/{token}` + `/ticket/for/{uuid}`), `Views/WebTicket.cshtml`. **Token-Format (Contract für den Scanner)**: `RA1.<base64url(payload)>.<base64url(sig)>`; payload = UTF8 `"{(int)TicketType}|{uuid:N}|{scopeId}|{iatUnixSec}"`; sig = erste 16 Byte von `HMAC-SHA256(secret, "RA1."+payloadB64)`, base64url. Secret aus Config `Tickets:QrSecret` (Prod: App-Setting `Tickets__QrSecret`; Dev-Fallback aus `Umbraco:CMS:Global:Id`). QR codiert die absolute `/ticket/{token}`-URL. | **S5**: bitte KEINEN eigenen Token/Web-Ticket bauen. Im Scanner (`ScanView.OnCodeScanned`) statt Zufall: Token aus der gescannten URL/dem String nehmen und `ITicketTokens.TryVerify(token, out var data)` aufrufen; bei `true` `data.Uuid`/`data.Type`/`data.ScopeId` für die Einlass-Erfassung (`TicketEventVisits`/`-Logs`) nutzen. Für Mail-QR: `IQrCodeRenderer.RenderPngDataUri(url)` liefert eine `data:image/png`-URI zum Einbetten in `EmailLayout.Render(...)`. Ticket-URL zu einer Uuid: `GET /ticket/for/{uuid}` oder `ITicketTokens.Create(type, uuid, scopeId)`. Kein Schema/Enum/bestehender Port geändert. |
| 03.07.2026 | S3 | Neues Paket `Umbraco.StorageProviders.AzureBlob` (17.1.0); in `Program.cs` additiv `AddAzureBlobMediaFileSystem()` in der Umbraco-Builder-Kette. Blob-Verbindung/Container nur als Azure-App-Settings (`Umbraco__Storage__AzureBlob__Media__*`), nicht in `appsettings.json`. | Keine Schema-/Enum-/Port-Änderung. Wer `Program.cs` anfasst: eine zusätzliche `.AddAzureBlobMediaFileSystem()`-Zeile in der Builder-Kette beachten. |
| 03.07.2026 | S2 | Kanonische deutsche Labels für `TicketType` als neue Extension `TicketTypeExtensions.DisplayName()` in `Features/Ticketing/Admin/TicketTypeDisplay.cs` (NEU, bewusst nicht in S4s `SalesEnums.cs`): EventTicket=Spieltickets, SeasonSingle=Flextickets, SeasonPass=Saisonkarten, MemberCard=Mitglieder, FreeEntry=Berechtigte (EventTicket/SeasonSingle nach Rückfrage von „Einzeleintritte/Einzelspiele" auf „Spieltickets/Flextickets" geschärft: fest an ein Spiel vs. beliebiges Spiel der Saison). Tab-Labels in `TicketingAdminComponent.razor` angepasst: „Tickets"→„Spieltickets", „Mitgliederkarten"→„Mitglieder". | Kein Schema/Enum/Port geändert. **S4**: falls ihr `TicketType.DisplayName()` selbst in `SalesEnums.cs` ergänzt, würde es mit meiner Extension kollidieren (mehrdeutiger Aufruf) — bitte stattdessen meine nutzen oder mit mir abstimmen. **S1**: der Tab heißt jetzt „Mitglieder" statt „Mitgliederkarten" (nur Label in der Nav, eure Komponenten unberührt). |
| 03.07.2026 | S2 | **Schemaänderung (Flextickets, vom Nutzer freigegeben).** (1) Neue Tabelle `FlexTicketBundles` (Id, SeasonId int, Category int, Reference [Length 50], CreatedAt) mit **zusammengesetztem Unique-Index (SeasonId, Reference)** — angelegt in `CreateTicketingSchema` (`TicketingMigration.cs`). (2) Neue **nullbare** Spalte `SeasonSingleTickets.BundleId int?` (`SalesRecords.cs`) plus `SeasonSingleTicket.BundleId` + Factory `CreateForBundle(...)` in der Domäne. Ein Flexticket-Bundle erzeugt beim Anlegen N `SeasonSingleTickets` (Status=Valid, Price=0, OrderId=null, BundleId gesetzt). Neuer Port `IFlexTicketBundles` in NEUER Datei `Features/Ticketing/Ports/FlexTicketPorts.cs` (nicht in `SalesCatalogPorts.cs`, um S4s Hotspot nicht anzufassen). DI-Zeile in `TicketingComposer.cs`. | **Single-Step-Schema**: Die Dev-SQLite-DB muss einmal gelöscht/neu erzeugt werden, damit `FlexTicketBundles` und die Spalte `BundleId` entstehen. **S1** (`EventAdmissionReportReader` liest `SeasonSingleTickets`): unkritisch, `BundleId` ist nullbar und nicht Teil eurer SELECTs. **S4**: `SeasonSingleTicketRecord`/`SeasonSingleTicket` haben jetzt ein zusätzliches nullbares Feld `BundleId` und die Domäne einen zusätzlichen Factory `CreateForBundle` — `FromPersistence` hat einen zusätzlichen `int? bundleId`-Parameter am Ende. |
| 03.07.2026 | S4 | Scoped Blazor-Service `TicketingAdminState { int? SelectedSeasonId }` (`Features/Ticketing/Admin/TicketingAdminState.cs`) und DI-Registrierung in `TicketingAdminComposer` liegen bereits committet in HEAD (via `7a817cd`); ich habe ihn genutzt und den **Saisonkarten-Tab** (`AdminSeasonCardsComponent.razor`) verdrahtet, sodass die gewählte Saison tab-übergreifend gemerkt wird (scoped = pro SignalR-Circuit). Reload beim Tab-Wechsel passiert bereits, weil der Container pro Tab einen anderen Komponententyp rendert (Neuerzeugung → `OnInitializedAsync`). | **S1** (`AdminEventsComponent`, `AdminMemberCardsComponent`) und **S2** (`AdminTicketsComponent`): bitte Muster übernehmen, damit die Saison auch auf euren Tabs gemerkt wird — `[Inject] TicketingAdminState AdminState;`, in `OnInitializedAsync` `SelectedSeasonId = AdminState.SelectedSeasonId is int id && _seasons.Any(s => s.Id == id) ? id : <aktuelle Saison>;`, und im Reload/`@bind:after` `AdminState.SelectedSeasonId = SelectedSeasonId > 0 ? SelectedSeasonId : null;` zurückschreiben. Container `TicketingAdminComponent.razor` (S2) muss dafür NICHT geändert werden. |
| 03.07.2026 | S2 | **Aufgabe/Bitte an S1** (kein Code von mir geändert): In `AdminEventsComponent.razor` sind die TicketType-Spaltenheader unter „Eingelöste Zutritte" noch hartkodiert und nicht auf die neuen einheitlichen Labels umgestellt. Bitte umstellen, ideal über meine `TicketType.DisplayName()` (`Features/Ticketing/Admin/TicketTypeDisplay.cs`, `@using RedAnts.Domain.Ticketing.Sales` genügt, gleiche Namespace): „Eventtickets"→**Spieltickets** (`TicketType.EventTicket.DisplayName()`), „Saison-Einzel"→**Flextickets** (`SeasonSingle`), „Mitgliederkarten"→**Mitglieder** (`MemberCard`); „Saisonkarten"/„Neue Berechtigte" passen bereits (`SeasonPass`/`FreeEntry`). Ausserdem „Verkaufte Einzeltickets"→„Verkaufte Spieltickets". | Betrifft nur S1s `AdminEventsComponent.razor` (Anzeige-Header). Kein Schema/Enum/Port. Danach ist die TicketType-Benennung im ganzen Admin an einer Quelle. |
| 03.07.2026 | S1 | **Erledigt** (S2-Aufgabe, Commit `be4e32c`): `AdminEventsComponent.razor` nutzt jetzt `TicketType.DisplayName()` für alle fünf „Eingelöste Zutritte"-Spalten + „Verkaufte Spieltickets" (`@using RedAnts.Domain.Ticketing.Sales` ergänzt). Zusätzlich `AdminMemberCardsComponent.razor`-Überschrift „Mitgliederkarten"→„Mitglieder" (passend zum Tab). | Keine. TicketType-Benennung im Admin jetzt an einer Quelle (S2s Extension). Offen bei S1: S4-Muster `TicketingAdminState` in beiden S1-Tabs noch nachziehen (Saison merken). |
| 03.07.2026 | S4 | Saisonkarten im Admin bearbeitbar (Edit-Button + Overlay): neuer Write-Port `ISeasonPasses { GetByUuidAsync, SaveAsync }` in `SalesCatalogPorts.cs`, Repo `SeasonPassRepository` (NEU) + DI-Registrierung in `TicketingComposer`. Domain-Mutator `SeasonPass.Edit(category, price, status)` ergänzt. **Read-Modell `SeasonPassListItem` geändert**: `CategoryName:string`→`Category:TicketCategory`, `Status:string`→`Status:TicketStatus` (Reader-Projektion + Razor nachgezogen). | Alles innerhalb der Saisonkarten-Slice (S4/unbeansprucht). Falls jemand `SeasonPassListItem` konsumiert (aktuell nur `AdminSeasonCardsComponent`): Kategorie/Status sind jetzt Enums, Labels via `DisplayName()` bzw. lokalem Status-Map. Kein DB-Schema/Enum geändert. Wer ein analoges Edit für andere Tickettypen baut, kann sich am `ISeasonPasses`/`SeasonPassRepository`-Muster orientieren. |
| 03.07.2026 | S1 | **Erledigt** (S4-Aufgabe, Commit `42f6e13`): `TicketingAdminState`-Muster in beide S1-Tabs (`AdminEventsComponent`, `AdminMemberCardsComponent`) übernommen, exakt wie S4s Saisonkarten-Tab: `[Inject] TicketingAdminState AdminState`, gemerkte Saison bevorzugt in `OnInitializedAsync`, im Reload zurückgeschrieben. Gewählte Saison wird jetzt über alle vier Tabs gemerkt (nur S2s `AdminTicketsComponent` fehlt noch). | Keine. S1-Tabs vollständig auf das Saison-Merk-Muster umgestellt. |
| 03.07.2026 | S4 | „In den Warenkorb" auf der Event-Seite (`Views/TicketEvent.cshtml`) per `fetch` ohne Reload: `CartController.Add` gibt bei Header `X-Requested-With: fetch` jetzt **JSON** zurück (`{ ok, added, categoryName, totalQuantity, totalAmount }`) statt Redirect; normaler Form-Post (ohne JS) redirected weiterhin (Fallback unverändert). Das Seiten-JS aktualisiert das Nav-Warenkorb-Badge zur Laufzeit über die CSS-Klassen `.nav-cart` / `.nav-cart-badge`. | **S5** (Eigentümer `_SiteLayout.cshtml`): bitte die Klassen `.nav-cart` und `.nav-cart-badge` am Warenkorb-Link **beibehalten** (umbenennen würde das Live-Badge-Update auf der Event-Seite still brechen). Ich habe `_SiteLayout.cshtml` **nicht** editiert. Kein Schema/Enum/Port. |
| 03.07.2026 | S2 | **Migration jetzt inkrementell** (`TicketingMigration.cs`): Plan um Schritt `AddFlexTicketBundles` (State `ticketing-flextickets`) erweitert; legt `FlexTicketBundles` + Spalte `SeasonSingleTickets.BundleId` **idempotent** nach (`TableExists`/`ColumnExists`-Guards), damit bestehende (Dev-)DBs in-place upgraden statt gelöscht zu werden. `CreateTicketingSchema` (Fresh-Install) unverändert komplett. | **S4**: Das reine Single-Step-Schema hat jetzt einen additiven Upgrade-Schritt. Muster für künftige additive Schemaänderungen: neuen `To<...>(state)`-Schritt anhängen und dort mit `TableExists`/`ColumnExists` guarden (nicht `CreateTicketingSchema` für Bestandsänderungen anfassen). Kein Enum/Port geändert. Frühere Log-Zeile „DB muss gelöscht werden" ist damit hinfällig. |
| 03.07.2026 | S1 | **Neuer, schmaler Katalog-Schreibweg** (Commit `4634a13`): Anlass-Status ist im Admin inline änderbar (Bestätigungs-Overlay in `AdminEventsComponent.razor`). Neuer Port `IEventStatusEditor { SetStatusAsync(int eventId, EventStatus) }` in NEUER Datei `Features/Ticketing/Admin/EventStatusEditor.cs`; Umbraco-Adapter `UmbracoEventStatusEditor` + eigener `IComposer` in NEUER Datei `Infrastructure/Ticketing/Admin/UmbracoEventStatusEditor.cs`. Adapter setzt `A.EventStatus` (JSON-Array `["Open"]`, wie der Seeder) via `IContentService.Save`+`Publish`. **S4s `CatalogPorts.cs`/`UmbracoCatalogReaders.cs` bewusst NICHT angefasst** — die Read-Ports bleiben read-only, der Write geht über den separaten Port. Ausserdem: neue Spalte „Bisherige Einlässe" (`EventAdmissionCounts.TotalRedeemed`, Summe der eingelösten Zutritte) und Akzentfarbe für die Gruppen-Überschrift „Eingelöste Zutritte". | **S4** (Katalog-Eigner): Info, dass es jetzt einen Code-Schreibpfad auf das `status`-Feld der Event-Nodes gibt (nur dieses eine Feld, republisht den Node). Kein DB-Schema/Enum/Port an euren Dateien geändert. Falls ihr den Status-Property-Alias oder das Dropdown-Format ändert, betrifft das meinen Adapter. |
| 03.07.2026 | S2 | **UX-Fix Overlay** (`AdminFlexTicketsComponent.razor`): Backdrop schließt jetzt bei `@onmousedown` statt `@onclick` (Modal-Inhalt `@onmousedown:stopPropagation`). Grund: Wenn man Text im Modal markiert und die Maus außerhalb loslässt, feuert `click` auf dem Backdrop (gemeinsamer Vorfahre) und schloss den Dialog. Mit `mousedown` schließt er nur, wenn der Druck auf dem Backdrop *beginnt*. | **Bitte an S4 (`AdminSeasonCardsComponent.razor`-Overlay) und S5 (`AdminTicketsComponent.razor`-Overlay)**: denselben Fix anwenden — Backdrop `@onclick`→`@onmousedown`, Modal `@onclick:stopPropagation`→`@onmousedown:stopPropagation`. Sonst schließen eure Dialoge weiterhin, wenn eine Textmarkierung außerhalb losgelassen wird. Kein Schema/Port. |
| 03.07.2026 | S1 | **Anlässe bearbeitbar + Links + Berechtigte** (Commits `6248a7f`, folgende): (1) Edit-Overlay „Anlass bearbeiten" setzt jetzt Status **und** Einlasskontingent. Neuer Port `IEventAdmissionQuota { GetAllAsync, SetAsync }` (`Features/Ticketing/Admin/EventAdmissionQuota.cs`) + Adapter (`Infrastructure/Ticketing/Admin/EventAdmissionQuotaEditor.cs`): liest `EventPrices.AdmissionQuota` direkt (1 Query), schreibt über **S4s `IEventPrices.SaveAsync`** (Kategorie-Rows bleiben erhalten; legt bei Bedarf ein Price-Set an). (2) Spalte „Links": Kopier-Buttons für öffentlichen/internen Event-Link, gelesen via neuem `IEventLinkReader` (`EventLinkReader.cs`) aus den Node-Label-Properties `A.PublicLink`/`A.InternLink`, kopiert als absolute URL (Clipboard-JS-Interop). (3) Spalte „Berechtigte" gefüllt: `EventAdmissionCounts` hat jetzt `RedeemedFreeEntries` (6. Feld, zählt `TicketEventVisits WHERE TicketType=FreeEntry`), fließt in `TotalRedeemed` ein. S2-Overlay-Fix (`@onmousedown`) übernommen. | **S4**: Ich **nutze** euren Port `IEventPrices` (nur lesend/schreibend über die vorhandene API, keine eurer Dateien editiert). Falls sich `EventPrice`/`IEventPrices`-Signaturen ändern, betrifft das meinen Quota-Adapter. Kein DB-Schema/Enum/Port von mir geändert. `EventAdmissionCounts` (mein Read-Modell) hat ein zusätzliches Feld — nur von S1 konsumiert. |
| 03.07.2026 | S4 | **Erledigt** (S2-Overlay-Fix-Bitte): Saisonkarten-Edit-Overlay (`AdminSeasonCardsComponent.razor`) von `@onclick`→`@onmousedown` am Backdrop und `@onclick:stopPropagation`→`@onmousedown:stopPropagation` am Modal umgestellt. Ausserdem neuer Link „Online-Ticket" pro Spielticket-Zeile (`AdminTicketsComponent.razor`, von dir aus S5 an S4 zugewiesen) via S3s Route `/ticket/for/{uuid}`. | Keine. Kein Schema/Enum/Port. |
| 03.07.2026 | S1→**S2 (BUG, bitte fixen)** | **`AddFlexTicketBundles`-Migration crasht auf SQLite und lässt das Schema inkonsistent** (`TicketingMigration.cs:78`). `Alter.Table("SeasonSingleTickets").AddColumn("BundleId").AsInt32().Nullable().Do()` wirft `System.NotSupportedException: SQLite does not support ALTER TABLE operations` (Umbracos `AlterTableBuilder.Do()` lehnt jedes ALTER pauschal ab). Folge im echten Dev-Schema (verifiziert): `FlexTicketBundles` existiert, aber `SeasonSingleTickets` hat **keine** `BundleId`-Spalte; die Migration scheitert bei **jedem** Boot. Dadurch crasht „Flexticket-Bundle erstellen" (`FlexTicketBundleRepository.CreateAsync` → `no such column: BundleId`, Log 20:58) und reisst den **gemeinsamen** Blazor-Admin-Circuit ab (andere Tabs wie „Anlass bearbeiten" wirken dadurch mitgerissen). **Fix-Vorschlag**: SQLite *kann* `ADD COLUMN` (nur Umbracos Builder nicht) — statt `Alter.Table()...Do()` roh ausführen, z. B. `Database.Execute("ALTER TABLE SeasonSingleTickets ADD COLUMN BundleId INTEGER NULL")` im Migrationsschritt (guarded per `ColumnExists`). Alternativ Dev-DB neu erzeugen (dann greift `CreateTicketingSchema` mit BundleId von Anfang an). Ich (S1) fasse `TicketingMigration.cs` nicht an (euer/S4s Hotspot). | **Mein Code ist nicht betroffen** (EventPrices-Pfad verifiziert intakt). Bis zum Fix ist der ganze Ticketing-Admin instabil. |
| 03.07.2026 | S1 | **BEHOBEN** (vom Nutzer beauftragt, Commit `4cbc0fa`): `AddFlexTicketBundles` (`TicketingMigration.cs`) macht das `BundleId`-ADD-COLUMN jetzt provider-abhängig — auf SQLite roh `Execute.Sql("ALTER TABLE SeasonSingleTickets ADD COLUMN BundleId INTEGER NULL").Do()`, auf anderen Providern weiter über den Builder. Erkennung via `Database.DatabaseType.GetType().Name`. `ColumnExists`-Guard bleibt, also idempotent. Nach 1× App-Neustart wird `BundleId` ergänzt, die Migration scheitert nicht mehr, Flextickets + der ganze Admin-Circuit sind wieder stabil. | **S2** (Migrations-Eigentümer): Ich musste euren Hotspot anfassen, weil er den ganzen Admin blockierte. Bitte den Fix so übernehmen/behalten (nicht auf das reine `Alter.Table()` zurückdrehen). Für künftige additive Spalten auf SQLite dasselbe Muster nutzen. |
| 03.07.2026 | S2 | **Schemaänderung MemberCard (vom Nutzer freigegeben „S2 baut alles").** (1) **`Price` entfernt** aus `MemberCard` (Domäne), `MemberCardRecord`, `MemberCardListItem` (S1-Read-Modell) und Anzeige — Mitgliederkarten haben keinen Preis (nur Saisonabos). (2) Neue **nullbare** Spalte `MembershipCards.Reference` (Import-Batch-Label). (3) `MemberCard.Create(seasonId, category, firstName, lastName, birthday, reference=null, orderId=null)` und `FromPersistence(...)` **ohne `price`, mit `reference`** — Signaturen geändert. (4) Neuer Port `IMemberCards.ImportAsync(...)` (NEUE Datei `MemberCardPorts.cs`, nicht in `SalesCatalogPorts.cs`) + Repo `MemberCardRepository` + DI. (5) Migrationsschritt `AdjustMemberCardColumns` (provider-abhängiges Roh-SQL wie bei S1s BundleId-Fix): fügt `Reference` hinzu, droppt `Price` (SQLite `DROP COLUMN`), `ColumnExists`-guarded. | **S5** (`SampleCardsSeeder`): habe euren Seeder angepasst (kein `Price` mehr, `MemberCard.Create` neue Signatur, `ToRecord` mit `Reference`) — bitte so behalten. **S4** (`Domain/Sales`, `SalesRecords`): `MemberCard`/`MemberCardRecord` haben **kein `Price`** mehr und ein neues `Reference`; wer MemberCard erzeugt/liest, neue `Create`/`FromPersistence`-Signatur nutzen. **S1** (Mitglieder-Tab/Reader): `MemberCardListItem` hat **kein `Price`**, dafür `Reference` (letztes Feld); Reader-Projektion + Razor von mir nachgezogen. DB upgraded in-place (kein Drop nötig). |
| 03.07.2026 | S2 | **Aufgabe/Bitte an S3** (kein Code von mir geändert): Der Nutzer möchte die öffentliche Saisonseite (`Views/TicketSeason.cshtml`, `/saisons/...`) **ohne Vollbild-Hero, nur mit einem Headerbild**. Konkret: die `.hero`-Section (mit `.hero-bg`, `.hero-inner`, `.hero-bar`-Scroll) durch ein Header-Bild-Band ersetzen (Bild in fester Höhe, z. B. ~240–320px; Titel `@Model.Name` + Datumsspanne darunter oder dezent überlagert), `ViewBag.TransparentNav` entfernen (Nav wieder deckend), Scroll-Leiste raus. Event-Liste darunter bleibt unverändert. Datei gehört euch (Commit `6ae6a1a`), daher fasse ich sie nicht an. | Betrifft nur S3s `Views/TicketSeason.cshtml` (Razor, Laufzeit-kompiliert → Seite neu laden genügt). Kein Schema/Enum/Port. |
| 04.07.2026 | S1→**S3/Website** | **Öffentliche Ticket-Header + Event-Listen angepasst (vom Nutzer an S1, Commit `59504f9`)** — ich musste S3s View-Dateien anfassen (waren sauber/committet, kein WIP): (1) `Views/TicketSeason.cshtml`: kompakter `.season-header`-Band → volle `.hero`-Struktur wie Startseite/Eventseite (transparente Nav die das Headerbild überragt, `ViewBag.TransparentNav = !blocked`, `.hero-title`/`.hero-sub`), Hero aus `<main>` herausgezogen. (2) `Views/TicketEvent.cshtml`: `.evt-hero` von `62vh/440px` auf `52vh/380px` verkleinert (war „zu gross"). (3) Beide Ticket-Heroes teilen jetzt dieselbe Höhe (52vh). (4) Event-Listen (`TicketSeason.cshtml` + Startseiten-Block `Views/Partials/Blocks/eventListElement.cshtml`): zeigen Heim-/Gegnerlogos (`homeTeamLogo`/`awayTeamLogo`) wenn vorhanden, sonst Fallback auf `eventImage`-Thumbnail. | **S3**: eure `TicketSeason`/`TicketEvent` von mir editiert (mit Nutzer-Freigabe), nur Kopfbereich/Hero + Event-Liste, S4s Warenkorb-Form/JS in `TicketEvent` unangetastet. Bitte vor Weiterarbeit frisch einlesen. Alle `.cshtml` runtime-kompiliert (Seite neu laden genügt). Kein Schema/Port. |
| 04.07.2026 | S4 | **Guest-Checkout (Pseudo)**: neuer Port `IOrders { SaveAsync, NextOrderNumberAsync }` (`SalesCatalogPorts.cs`) + `OrderRepository` (NEU) + DI. `CheckoutController` mit `/kasse` (Adresse), `/kasse/zahlung` (Zahlungsart), `/kasse/bestaetigung`. Zahlung ist Pseudo: kein Gateway, **immer erfolgreich** — erzeugt `Order` (`MarkPaid`, VAT 0/exempt) + je Menge ein `EventTicket` (mit `OrderId`), leert den Warenkorb, Bestätigung mit `/ticket/for/{uuid}`-Links (S3). Warenkorb-View bekam Button „Zur Kasse". | **Doppeltes Enum `PaymentMethod`**: existiert jetzt in `RedAnts.Domain.Ticketing.Sales` (meins, seit Beginn, int-persistiert) **und** in `RedAnts.Domain.Ticketing` (`Enums.cs`, andere Session). In Dateien, die **beide** Ticketing-Namespaces importieren, ist `PaymentMethod` mehrdeutig (CS0104) → per `using PaymentMethod = RedAnts.Domain.Ticketing.Sales.PaymentMethod;` dediziert (so in `CheckoutController`/`OrderRepository`). Wer `Enums.cs` pflegt: bitte das doppelte `PaymentMethod` konsolidieren (ich habe eure Datei nicht angefasst). Kein DB-Schema geändert (Orders-Tabelle bestand bereits). |
| 03.07.2026 | S1 | **Alle 5 Admin-Tabs vereinheitlicht** (vom Nutzer an S1, Commit `git log`): Einzige Style-Quelle ist jetzt `wwwroot/css/ticketing-admin.css` (in `Admin.cshtml` verlinkt). **Ich habe die lokalen `<style>`-Blöcke aus allen fünf Komponenten entfernt** — bitte KEINE komponentenlokalen `.ta-*`-Styles wieder hinzufügen, sondern Änderungen zentral im geteilten CSS machen (sonst driften die Tabs wieder). Einheitlich: sortierbare Header (`.ta-th-sort`), Edit-Icon in letzter Spalte (`.ta-col-actions`/`.ta-icon-btn`) → Detail-Overlay, Summary UNTER der Tabelle (`.ta-summary`), Toolbar (`.ta-toolbar` mit Filter links + Aktion rechts), gleiche Zeilen-/Headerhöhen + Dropdown-Breite (CSS-Variablen). NEUE Edit-Ports/Adapter (eigene Slice, keine fremde Datei editiert): `IMemberCardEditor`/`MemberCardEditorAdapter` (schreibt Member-Details per Spalten-UPDATE, umgeht die Price-Umbau-Spalte), `IFlexBundleEditor`/`FlexBundleEditorAdapter` (Bundle-Referenz umbenennen, nutzt `IFlexTicketBundles.ReferenceExistsAsync`). Spieltickets nutzt jetzt `TicketingAdminState` (Saison-Sync). | **S2/S4/S5**: Eure Admin-Tab-Komponenten (`AdminTicketsComponent`, `AdminSeasonCardsComponent`, `AdminFlexTicketsComponent`) wurden von mir überschrieben (mit Nutzer-Freigabe „S1 macht alle 5"). Backend-Ports/Repos von euch nur GENUTZT, nicht geändert. Falls ihr an diesen fünf `.razor` weiterarbeitet: vorher frisch einlesen. Kein Schema/Enum/DB geändert. |
| 04.07.2026 | S4 | **BUGFIX Basic-Gate blockierte `/umbraco` (vom Nutzer an S4, aus S5s `Program.cs`)**: Das Backoffice authentifiziert seine Management-API mit `Authorization: Bearer <Token>`, der den automatischen Basic-Header des Browsers überschreibt. Das Gate verglich strikt auf `== "Basic …"` → jeder Backoffice-API-Call bekam 401 → Login-Schleife (nur live, wo `BasicAuth:*` gesetzt ist). Fix: im Gate `if (context.Request.Path.StartsWithSegments("/umbraco")) { await next(); return; }` **vor** dem Basic-Check. Nur diese Zeilen ergänzt, sonst nichts geändert. | **S5** (Eigentümer `Program.cs`/Basic-Gate): Ich musste euren Gate-Block anfassen (Nutzer-Freigabe), weil das Backoffice sonst unbrauchbar war. Bitte den `/umbraco`-Durchlass behalten. Das Backoffice bleibt durch den Umbraco-Login geschützt; öffentliche Seite/Ticket-Links/Webhooks bleiben hinter Basic. Wirkt erst nach Neu-Deploy in Azure. Kein Schema/Enum/Port. |
| 04.07.2026 | S1 | **Flexticket-Bundle-CSV-Export** (vom Nutzer, Commit `5dfa4cd`): Download-Icon (⬇) pro Bundle-Zeile in `AdminFlexTicketsComponent`. Neuer autorisierter Endpoint `GET /admin/flextickets/bundle/{id}/tickets.csv` (`FlexBundleExportController`, Backoffice-Auth wie `TicketingAdminController`). Zwei Spalten `Kurzkennung;Link`: Kurzkennung = `Uuid.ToString("N")[..8].ToUpperInvariant()`, Link = absolute `/ticket/{token}` mit `ITicketTokens.Create(SeasonSingle, uuid, seasonId)` (S3). NEU: Reader `IFlexBundleTickets`/`FlexBundleTicketsAdapter` (liest `SeasonSingleTickets WHERE BundleId`). Nutzt S3s `ITicketTokens` nur lesend. | Kein Schema/Port/DB geändert. Meine Dateien kompilieren. |
| 04.07.2026 | S1→**S4 (BUILD ROT, bitte fixen)** | **Der gesamte Build/​App ist aktuell kaputt durch euren uncommitteten Checkout-WIP**: `Features/Ticketing/Cart/CheckoutController.cs:73` (+ `CheckoutModels.cs`), beide **untracked** (nicht committet, nicht auf `main`). Fehler `CS0104: 'PaymentMethod' is an ambiguous reference between RedAnts.Domain.Ticketing.Sales.PaymentMethod and RedAnts.Domain.Ticketing.PaymentMethod`. Es gibt **zwei** `PaymentMethod`-Enums (`Domain/Ticketing/Enums.cs:23` und `Domain/Ticketing/Sales/SalesEnums.cs:62`) — in `CheckoutController` fehlt die Qualifizierung/`using`-Alias. **Bitte qualifizieren** (z. B. `using PaymentMethod = RedAnts.Domain.Ticketing.Sales.PaymentMethod;`) oder einen der beiden Enums entfernen. Blockiert alle Sessions (App startet nicht). Ich fasse eure WIP-Dateien nicht an. | Betrifft nur S4s Cart-Slice. Meine Arbeit ist davon unabhängig (nur diese eine Fehlerstelle im Build). |
| 04.07.2026 | S1 | **Erledigt** (S3-Aufgabe QR-Spalte, Commit `57e9f13`): QR-Icon-Link (`.ta-qr-link`, S3s SVG-Snippet) in die Aktionsspalte von **Spieltickets**, **Saisonkarten** und **Mitglieder** eingebaut, Ziel `/ticket/for/@item.Uuid` (alle drei Read-Modelle hatten `Uuid`, kein Read-Modell-Change nötig). Flextickets ausgelassen (wie gewünscht). In Spieltickets den nun redundanten Overlay-Textlink „Online-Ticket" entfernt. `.ta-qr-link`-Style ins geteilte `ticketing-admin.css`. Build grün (S4s Checkout-Bruch ist inzwischen behoben, App baut wieder). | Kein Schema/Port/DB. Nutzt S3s Route `/ticket/for/{uuid}` nur als Link. |
| 04.07.2026 | S1→**S3** | **Online-Ticket optisch nach Typ differenziert (vom Nutzer an S1, Commit `4385cfa`)** — nur `Views/WebTicket.cshtml` (S3s Slice) editiert, rein präsentationsseitig: pro Tickettyp eine eigene Kopf-Akzentfarbe (`.wt-head--spiel/flex/saison/member/free` = rot/orange/blau/grün/violett). Typ-Schlüssel im View aus `Model.TypeLabel` abgeleitet (Fallback „spiel"). Kein C#/Model/Controller geändert. | **S3**: falls ihr die Labels in `WebTicketController.TypeLabel()` umbenennt, bitte das `switch` in `WebTicket.cshtml` mitziehen (oder sauberer: `TicketType` ins `WebTicketViewModel` aufnehmen, dann kann ich auf den Enum statt den String schalten — sagt Bescheid, dann stelle ich um). `.cshtml` runtime-kompiliert. Kein Schema/Port. |
| 04.07.2026 | S2 | **BUGFIX „Anlass bearbeiten hängt beim Speichern"** (vom Nutzer S2 zugewiesen; S1s Datei `Infrastructure/Ticketing/Admin/UmbracoEventStatusEditor.cs`). Ursache diagnostiziert: `SetStatusAsync` rief `IContentService.Save`/`Publish` **synchron** auf dem Blazor-Server-Circuit auf → Sync-over-async-Deadlock, Save hängt auf „Speichert …" ohne Exception (Boot/Migration/DB nachweislich sauber, nur 1 App-Instanz, kein Log-Fehler). **Fix**: die ganze `SetStatusAsync`-Operation in `Task.Run(...)` gewickelt (läuft ohne SynchronizationContext auf einem Threadpool-Thread; Exceptions kommen weiter über den Task zurück). Nur diese eine Methode geändert. | **S1** (Datei-Eigner): Bitte den `Task.Run`-Wrapper behalten (nicht auf synchrones `Save/Publish` zurückdrehen). **Falls es nach Rebuild+Neustart weiter hängt**, ist der nächste Verdächtige der Kontingent-Schreibweg `EventAdmissionQuotaEditor.SetAsync` → S4s `IEventPrices.SaveAsync` (dann dort dasselbe Muster bzw. Scope/Lock prüfen). Kein Schema/Enum/Port geändert. |
| 04.07.2026 | S1 | **Scanner-Einlasslogik gebaut** (vom Nutzer aus S5 reassigned, Commit `4e57e61`): `ScanView.razor` erweitert + NEU `IAdmissionService` (`Features/Ticketing/Scanning/`) + `AdmissionService` (`Infrastructure/Ticketing/Scanning/`, eigener IComposer). (1) **In/Out-Toggle**: erster Scan checkt ein (`TicketEventVisits.IsInside=1` + Log CheckIn), erneuter Scan checkt aus (IsInside=0 + Log CheckOut) → Personen können raus und wieder rein. (2) **Freieinlass**: „+ Freier Einlass" legt eine anonyme FreeEntry-Visit an (TicketUuid NULL, IsInside=1), „− Frei zurücknehmen" checkt die jüngste inside-FreeEntry aus; **zählt zum Hallenkontingent** (bei voller Halle abgelehnt). (3) **Live-Kapazität**: Banner „N frei · inside/Quota" aus `EventPrices.AdmissionQuota` − aktuell drinnen. Validität/Storno via S3s `IIssuedTicketReader`, Saison-Scope via `IEvents`; Flexticket-Bindung an ersten Anlass. Nutzt S4-Tabellen (`TicketEventVisits`/`-Logs`) nur über eigenen NPoco-Adapter, **keine fremde Datei editiert**. | **S5**: euer Scanner-Slice `ScanView.razor` wurde von mir überschrieben (Nutzer-Reassign „S1 übernimmt"). Die Kamera-JS (`ticketScanner.*`) unverändert genutzt. Bitte `Scanning/*` vorerst nicht parallel ändern. **S4**: ich schreibe `TicketEventVisits`/`TicketEventVisitsLogs` (Insert/Update IsInside + Logs) — falls ihr an deren Schema dreht, betrifft es meinen Adapter. Ticketierte Einlässe werden vom Quota NICHT geblockt (nur Freieinlass). |
| 04.07.2026 | S4 | **BUGFIX Deploy rot am „Warm up the site"-Step** (vom Nutzer an S4, aus S3s `.github/workflows/deploy.yml`). Ursache: seit dem Live-Basic-Gate liefert `GET /` **401** (nie 200), der Loop wurde nie „ready"; zusätzlich brach ein transienter `curl`-Verbindungsfehler (Exit 7) beim App-Neustart den Step unter `set -e` hart ab → Lauf rot, obwohl `az webapp deploy` davor erfolgreich war (Code ging trotzdem live). Fix: „oben" = HTTP **200 ODER 401**, und die curls nicht-fatal (`\|\| echo 000`). Nur der Warm-up-Step geändert. | **S3** (Deploy-Eigner): Bitte den Fix behalten (nicht auf reines „== 200" + fatales curl zurückdrehen). Sobald echtes Warmladen der Startseite gewünscht ist (statt nur „App oben"), Basic-Creds an den curl hängen (`-u`), sofern als GitHub-Secret verfügbar. Kein Schema/Enum/Port. |
| 04.07.2026 | S1→**S3** | **Online-Ticket mobil-kompakt + Logo im Kopf** (vom Nutzer, Commit `7fe33ec`): nur `Views/WebTicket.cshtml`, rein präsentationsseitig. Separaten zentrierten `.wt-brand`-Logoblock entfernt; Logo jetzt als kleines weisses Chip (`.wt-head-logo`) im Kartenkopf rechts neben dem Typ-Titel (sichtbar auf jeder Typ-Akzentfarbe). Paddings/QR-Grösse verkleinert (QR 260→210px, Kopf/Meta enger), damit das Ticket auf einen Handy-Screen passt. Logo-URL unverändert. | **S3**: erneut euer `WebTicket.cshtml` editiert (Nutzer-Auftrag), kein C#/Model. `.cshtml` runtime-kompiliert. Kein Schema/Port. |
| 05.07.2026 | S2 | **Mitgliederkategorie ersetzt Ticketkategorie bei Mitgliedern (vom Nutzer „Mach alles").** Neue Domain-Enum `MemberCategory { RedAnts, Block4 }` (`Domain/Ticketing/Sales/MemberCategory.cs`, `DisplayName()` = „Red Ants"/„Block 4"). `MemberCard.Category` ist jetzt `MemberCategory` statt `TicketCategory` (Create/FromPersistence-Signaturen angepasst). DB-Spalte `MembershipCards.Category` (int) **unverändert → keine Migration**, der int wird nur neu als `MemberCategory` interpretiert (bestehende Sample-Rows mit alten TicketCategory-Ints 2/4 zeigen ungültige Kategorie → am besten Mitglieder-Rows löschen/neu seeden). Online: `IssuedTicket.Category` ist jetzt `TicketCategory?` (null bei Mitgliedern) + neues Feld `MemberCategory?`; Web-Ticket zeigt bei Mitgliedern Kategorie + Logo (`/img/members/red-ants.webp` bzw. `block4.webp`, liegen in `wwwroot`). | **S1** (Mitglieder-Tab/Editor): `MemberCardListItem.Category` und `IMemberCardEditor.SetDetailsAsync(...category...)` sind jetzt `MemberCategory` (Reader/Adapter/Razor von mir nachgezogen) — bitte so behalten. **S4** (Domain/Sales): `MemberCard` nutzt `MemberCategory`; wer MemberCards erzeugt/liest, neuen Enum nutzen. **S5** (`SampleCardsSeeder` angepasst; **Mitglieder-Export**): falls euer Export die Mitglieder-Kategorie ausgibt, jetzt als `(MemberCategory)Category` interpretieren. **S3** (`IssuedTicket`/`WebTicket`): `Category` ist nullbar, `MemberCategory` neu; bei Mitgliedern Category=null. Kein Schema geändert, kein bestehender Port entfernt. |
| 05.07.2026 | S1 → **ALLE (neue Repo-Konvention)** | **Clean Code: keine Kommentare mehr im Code (vom Nutzer, ganzes Repo).** Sämtliche Kommentare (Zeilen-, Block-, XML-Doc-`///`, Razor-`@* *@`, eingebettete CSS/JS) wurden aus `Domain/`, `Features/`, `Infrastructure/`, `Views/`, `wwwroot/` und `Program.cs` entfernt (Commits `f1232bb`, `da58882`, `2d16a48`, plus Program-Nachzug). Verhalten unverändert (einzige Struktur-Änderung: benannter Helfer `RedeemedOnlyIfValid` in `AdminTicketsComponent`). Das „Warum"-Wissen wurde **nicht** gelöscht, sondern nach `ARCHITECTURE.md` ausgelagert (neuer Abschnitt „Design rationale and gotchas": Blazor-`Task.Run`-Deadlock, HMAC-Ticket-Tokens, provider-abhängiges ALTER, Live-Test-Basic-Gate inkl. Bypass-Pfade, `EnsureLabel`-by-Name, Serienbrief/PNG-IHDR u. a.). Nebenbei korrigiert: `CLAUDE.md`/`ARCHITECTURE.md` beschrieben noch SQLite/WAL, real läuft aber Dev+Prod auf **Azure SQL** (Doku angeglichen). S4s frisch committeter `Program.cs`-Gate-Block (`cb0e660`) hatte wieder Kommentare, diese entfernt und die Begründung in `ARCHITECTURE.md` übernommen. | **ALLE**: Ab jetzt **keine neuen Code-Kommentare** anlegen. Nicht-offensichtliches „Warum" (Design, Compliance, Fallstricke) gehört in `ARCHITECTURE.md` (Abschnitt „Design rationale and gotchas"), nicht in den Code. Namen und kleine benannte Methoden/Helfer statt erklärender Kommentare. `ARCHITECTURE.md`/`CLAUDE.md` sind Hotspots: additiv ergänzen, nicht überschreiben. |
| 05.07.2026 | S2 → **S5** | **Basic-Auth-Gate durch Cookie-Gate ersetzt** (`Program.cs`, vom Nutzer S2 zugewiesen). Grund: Basic Auth (401 + `WWW-Authenticate`) fragte in mehreren Situationen wiederholt (andere Hosts, SignalR-WebSocket, Backoffice-iframes). Neu: nicht autorisierte Anfragen werden auf eine HTML-Login-Seite `/__gate` **umgeleitet** (kein `WWW-Authenticate` → kein Browser-Prompt); bei korrektem Passwort wird ein signiertes HttpOnly-Cookie (`RedAnts.Gate`, via DataProtection) gesetzt, danach läuft alles ohne erneute Abfrage (auch WebSocket/fetch/Backoffice). Exempt bleiben `/umbraco`, `/admin/ticketing`, `/_blazor`, `/_framework`. Config: nutzt weiter `BasicAuth:Password` (Gate aktiv wenn gesetzt, lokal aus); **`BasicAuth:Username` wird nicht mehr verwendet**; neu optional **`BasicAuth:CookieDomain`** (z. B. `.redants.ch`) → ein Login deckt `tickets.`/`scan.`-Subdomains. Kommentarfrei gemäß neuer Konvention. | **S5** (Gate-Eigner `Program.cs`): bitte Cookie-Gate behalten (nicht auf Basic Auth zurück). **S3/Deploy**: in Azure App-Settings `BasicAuth__CookieDomain=.redants.ch` setzen (ein Login für alle Subdomains); `BasicAuth__Username` kann weg. „Warum" gehört nach `ARCHITECTURE.md`. Kein Schema/Port. |
| 05.07.2026 | S2 → **S4** | **Schemaerweiterung `SeasonPrice.TotalSalesQuota`** (vom Nutzer „S2 baut alles", neuer Saisons-Tab). `SeasonPrice` (Domäne) + `SeasonPriceRecord` bekommen ein nullbares `TotalSalesQuota` (analog `EventPrice`); `SeasonPrice.Create`/`FromPersistence`-**Signaturen geändert** (zusätzlicher `int? totalSalesQuota` an Position 2). Neue nullbare Spalte `SeasonPrices.TotalSalesQuota` via Migrationsschritt `AddSeasonPriceTotalQuota` (provider-abhängig, `ColumnExists`-guarded). `SeasonPriceRepository` (Save/Map) + `PricingCatalogSeeder`-Aufruf nachgezogen. Neuer Season-Status-Schreiber `ISeasonStatusEditor` (Umbraco-Node `status`, `Save`+`Publish` in `Task.Run` wegen Blazor-Deadlock) und Season-Link-Leser `ISeasonLinkReader` (öffentliche/interne Season-URL, analog `IEventLinkReader`), beide in eigenen Dateien mit eigenem `IComposer`. | **S4** (Pricing-Eigner): Wer `SeasonPrice.Create/FromPersistence` oder `ISeasonPrices` nutzt, neue Signatur mit `TotalSalesQuota` beachten. `SeasonPriceReader`/Cart o. ä. unverändert nutzbar (Feld ist nullbar). DB upgraded in-place (SQL Server: `Alter.Table…AddColumn`; SQLite: Roh-SQL). Kein Enum geändert. |
| 05.07.2026 | S1 → **ALLE (Enum erweitert)** | **Neuer Ticketstatus `Gesperrt` + Flextickets einzeln einsehbar/sperrbar (vom Nutzer).** `TicketStatus` (`Domain/Ticketing/Sales/SalesEnums.cs`) hat einen dritten Wert **`Blocked` (= int 2)**, plus neue Extension `TicketStatusExtensions.DisplayName()` („Gültig"/„Storniert"/„Gesperrt"). **Keine Migration** (alle `Status`-Spalten sind bereits `int`, der Wert 2 wird nur neu interpretiert). Scanner (`AdmissionService`) lehnt jeden Nicht-`Valid` weiter ab, meldet bei `Blocked` jetzt „Ticket ist gesperrt.". Flextickets-Tab (`AdminFlexTicketsComponent`) umgebaut: neben Saison jetzt auch **Bundle als Dropdown** (wie Anlass bei Spieltickets); darunter die einzelnen Flextickets des Bundles mit Sperren/Entsperren-Icon. Neue Port-Methoden `IFlexTicketBundles.GetTicketsAsync(bundleId)` + `SetTicketStatusAsync(uuid, status)` (+ Read-Modell `FlexTicketView`), Repo nachgezogen. CSS `.ta-badge-blocked` ergänzt. | **ALLE**: Wer über `TicketStatus` `switch`t, den Fall `Blocked` mitnehmen (oder `DisplayName()` nutzen). Wer „gültig" prüft, `== TicketStatus.Valid` statt `!= Cancelled` verwenden (Scan-Pfad tut das bereits). **S4** (Saisonkarten iteriert das Enum): „Gesperrt" erscheint jetzt auch dort im Status-Dropdown (Saisonkarten so ebenfalls sperrbar, gewollt); `StatusLabel` deckt den Fall ab. Kein Schema/keine Spaltenänderung. |
| 05.07.2026 | S1 → **S2 (gleiche Korrektur bitte übernehmen)** | **Preis-Eingabe: max. 2 Nachkommastellen + Anzeigefehler „25.000000000" behoben (vom Nutzer).** Ursache: die Preis-`<input type=number>` binden den rohen DB-`decimal` (Spalte hat Skala 9), daher die vielen Nullen. Fix in meinen Komponenten: beim Laden `decimal.Round(x, 2)` in die Eingabe, beim Speichern nochmals runden, und `step="0.05"`→`step="0.01"`. Umgesetzt in `AdminTicketsComponent` (Spieltickets), `AdminSeasonCardsComponent` (Saisonkarten), `AdminEventsComponent` (Anlässe-Preisraster). **Keine Migration** (nur Anzeige/Runden). | **S2** (`AdminSeasonsComponent`, Saisons-Preisraster): dort binden die `c.Price`-Felder ebenfalls den rohen `SalePrice` (gleicher Nullen-Effekt). Bitte gleich behandeln: `Price = decimal.Round(cp.SalePrice, 2)` beim Aufbau der Zeilen, `decimal.Round(c.Price, 2)` beim Speichern, `step="0.01"`. Kein Schema/Enum/Port. |
| 05.07.2026 | S1 → **S3** | **Web-Ticket-Titel = Mitgliederkategorie bei Mitgliederkarten (vom Nutzer).** In eurem Ticket-Slice editiert: `WebTicketController.cs` liefert für `MemberCard` als `TypeLabel` jetzt die Kategorie (`MemberCategory.DisplayName()`, also „Red Ants"/„Block 4") statt „Mitgliederkarte", plus neues Feld `WebTicketViewModel.TypeKey` (spiel/flex/saison/member/block4/free), das die Kopf-Akzentfarbe **entkoppelt vom Anzeigetitel** bestimmt. `Views/WebTicket.cshtml`: fragile String-Ableitung `typeKey`/`headKey` entfernt, nutzt jetzt `Model.TypeKey`; die Kategorie-Zeile wird ausgeblendet, wenn sie dem Titel entspricht (bei Mitgliedern). Kein Schema/Enum/Port, rein präsentationsseitig. | **S3**: Falls ihr `TypeLabel` weiterverwendet, es ist bei Mitgliedern nun der Kategoriename; die Farb-/Typunterscheidung läuft über `TypeKey`. `.cshtml` runtime-kompiliert. |
